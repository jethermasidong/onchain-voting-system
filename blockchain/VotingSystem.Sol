// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract VotingSystem {

    struct Receipt {
        uint256 voters_id;
        uint256[] candidate_id;
        string receipt_hash;
        bool exits;
    }

    mapping(string => Receipt) private receipts;

    function registerReceipt(uint256 voters_id, uint256[] memory candidate_id, string memory receipt_hash, string memory tx_hash) public {
        
    }
}